Hardcodar o TikTok Pixel ID `D7TMC2JC77U4TTGIIJ70` em `src/lib/tiktokPixel.ts` (substituir a leitura de `VITE_TIKTOK_PIXEL_ID` por constante pública, mesmo padrão do `META_PIXEL_ID` em `metaPixel.ts`). Isso finaliza a implementação de tracking TikTok já feita (Pixel client-side + Events API server-side com dedup).